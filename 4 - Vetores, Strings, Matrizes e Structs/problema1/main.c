#include <stdio.h>
#include <stdlib.h>

#define TAM 20

struct livro {

    int codico;
    char titulo[50];
    char autor[30];
    char area[30];
    int ano;
    char editora[30];

};

int main()
{
    struct livro ficha[TAM];
    struct livro troca;

    int busca, i, j, acha, op;

    op = 0;

    while (op != 5) {

    }

    return 0;
}
